static void reverse() {
		Node prev= null;
		Node current = head;
		
		while(current != null) {
			Node next = current.next;   // saving the next pointer
			current.next = prev; // reverse the link
			prev = current; // move prev
			current = next; // move the current
		}
		head = prev;
	}
